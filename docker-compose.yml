
version: '3.8'
services:  
    app:
        build: .
        ports:
          - "${PORT}:${PORT}"
        depends_on:
          - redis
    redis:
        image: "redis:alpine"
        container_name: redis
